{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may call this app's own commands and nothing else. No plugin grants any general-purpose filesystem, shell, or HTTP capability to the webview: reading archives, opening links, and invoking `munshi` all go through validated commands in `src/lib.rs`.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
